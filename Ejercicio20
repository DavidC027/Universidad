using System;

class Program
{
    static void Main()
    {
        double myDouble = 5.48;
        int myInt = (int)myDouble;

        Console.WriteLine(myDouble);
        Console.WriteLine(myInt);
    }
}
